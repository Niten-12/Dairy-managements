# Keep the build context small. The native Android project in particular is
# irrelevant to the nginx image and, once built locally, drags in a large
# android/app/build and .gradle tree.
node_modules
dist
android

.env
.env.*
!.env.example
!.env.production.example

npm-debug.log*
*.log
.DS_Store
